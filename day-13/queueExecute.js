class queue{
    constructor(data){
        this.data = data
    }
    randomTime(){
        return Math.floor(Math.random()*10)
    }
    run(){
        let i = 0
        while(i<this.data.length){
            let promisData = new Promise()
            
        }
    }
}